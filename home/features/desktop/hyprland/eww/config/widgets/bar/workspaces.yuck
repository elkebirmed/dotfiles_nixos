(defwidget workspaces []
  (box
    :orientation "horizontal"
    :halign "center"
    :class "os"
    (box
      (eventbox
        (box
          (for workspace in {workspaces.workspaces}
            (button
              (label :text ""))))))))


